# Supabase frontend connection
# Copy to .env.local and fill from Supabase Studio → Settings → API
SUPABASE_URL=
SUPABASE_ANON_KEY=
# service_role key NEVER goes here. It bypasses RLS. Server-side only.
